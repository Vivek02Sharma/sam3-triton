[project]
name = "sam3-triton"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vivek02Sharma", email = "vivektusharma@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "opencv-python-headless>=5.0.0.93",
    "transformers>=5.15.0",
]

[project.scripts]
sam3-triton = "sam3_triton:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
